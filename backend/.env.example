# BSMarker Environment Configuration Template
# Copy this file to .env and update all values with secure secrets
#
# CRITICAL SECURITY NOTICE:
# - ALL values shown here are EXAMPLES ONLY
# - NEVER use these values in production
# - Generate new secrets using: python app/core/generate_secrets.py
# - Do NOT commit .env files to version control

# =================================================================
# JWT Authentication Settings (REQUIRED)
# =================================================================
SECRET_KEY=REPLACE_WITH_SECURE_64_CHAR_SECRET_FROM_GENERATOR
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =================================================================
# Database Configuration (REQUIRED)
# =================================================================
DATABASE_URL=postgresql://SECURE_USER:SECURE_PASSWORD@localhost:5432/bsmarker_db

# =================================================================
# Redis Configuration (REQUIRED)
# =================================================================
REDIS_URL=redis://:SECURE_REDIS_PASSWORD@localhost:6379/0

# =================================================================
# MinIO Storage Configuration (REQUIRED)
# =================================================================
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=SECURE_ACCESS_KEY_FROM_GENERATOR
MINIO_SECRET_KEY=SECURE_SECRET_KEY_FROM_GENERATOR
MINIO_SECURE=false
MINIO_BUCKET_RECORDINGS=recordings
MINIO_BUCKET_SPECTROGRAMS=spectrograms

# =================================================================
# Initial Admin User (REQUIRED)
# =================================================================
FIRST_ADMIN_EMAIL=admin@bsmarker.com
FIRST_ADMIN_PASSWORD=SECURE_PASSWORD_FROM_GENERATOR

# =================================================================
# CORS Settings
# =================================================================
CORS_ORIGINS=["http://localhost:3456"]

# =================================================================
# Application Settings (Optional - have defaults)
# =================================================================
PROJECT_NAME=BSMarker
VERSION=1.0.0
API_V1_STR=/api/v1

# File Upload Settings
MAX_UPLOAD_SIZE=104857600
ALLOWED_AUDIO_EXTENSIONS=[".mp3", ".wav", ".m4a", ".flac"]

# Spectrogram Generation Settings
SPECTROGRAM_N_FFT=2048
SPECTROGRAM_HOP_LENGTH=512
SPECTROGRAM_N_MELS=128

# =================================================================
# Production Deployment Notes:
# =================================================================
# 1. Generate secrets: cd backend && python app/core/generate_secrets.py
# 2. Validate config: python scripts/validate_config.py
# 3. Update database and MinIO with new credentials
# 4. Test configuration with: python scripts/validate_config.py --test-connections
# 5. For secret rotation: python scripts/rotate_secrets.py
